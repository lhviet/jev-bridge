## What this changes

<!-- One change per pull request. -->

## How I verified it

<!-- What did you run? If this touches the cache or the MCP protocol,
     say what you tried against a real MCP client, not only the tests. -->

## Checklist

- [ ] `npm test` passes.
- [ ] A new behaviour has a test that fails without this change.
- [ ] No runtime dependency added.
- [ ] Nothing new is written to stdout except protocol messages.
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`.
