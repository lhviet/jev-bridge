# Secrets. A TypeSafe key must never be committed.
.env
.env.*
!.env.example

# The local answer cache, usage log and call history.
*.db
*.db-wal
*.db-shm

# Exports from the history hold the states that were asked about:
# evaluation sets (examples/eval-set.mjs) and spreadsheets.
*.jsonl
jev-calls.csv
jev-history.json

node_modules/
coverage/
*.log
.DS_Store
