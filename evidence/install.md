# The install guide, followed

Every step of the [README](../README.md#install)'s install guide, run as written on 2026-09-21 by a new user with an empty home directory. **Generated by [`install.mjs`](install.mjs); do not edit by hand.**

**28 of 28 checks passed** over 13 steps.

| | |
| --- | --- |
| Machine | macOS 27.0, arm64 |
| Node | v24.21.0 |
| git | 2.53.0 |
| Claude Code | 2.1.278 (Claude Code) |
| Installed | `2db4bbe [security\|docs] Harden key handling and data files; Quick start; install guide and examples run as evidence (#2)`, cloned from https://github.com/lhviet/jev-bridge.git |

**How it was run.** A temporary directory stood in for a new machine: `~` below is its home directory, empty at the start, and `<clone>` is where the repository was cloned. Each command was read out of the README and run as a shell would. The key was written where Option A puts it, and is shown as `<key>`. The Claude Code session is the one step with this machine's real home directory, since that is where Claude Code keeps its login. It was given only the `jev` entry that `claude mcp add` wrote (`--strict-mcp-config`), with hooks and skills turned off, and the server kept its data in the sandbox (`JEV_BRIDGE_HOME`). In an interactive session Claude Code asks before a tool runs; `--allowedTools mcp__jev` answers yes in advance. The sandbox, and the copy of the key in it, were deleted at the end.

| Step | README section | Checks |
| --- | --- | --- |
| 1. Quick start, in one go | [Quick start](../README.md#quick-start) | ✔ 3/3 |
| 2. Node.js 18 or newer | [Prerequisites](../README.md#prerequisites) | ✔ 1/1 |
| 3. Clone | [Install](../README.md#install) | ✔ 2/2 |
| 4. Run the tests | [Install](../README.md#install) | ✔ 2/2 |
| 5. Note the server's absolute path | [Install](../README.md#install) | ✔ 1/1 |
| 6. Write the key file | [Option A — a key file (recommended)](../README.md#option-a--a-key-file-recommended) | ✔ 3/3 |
| 7. Register the server with Claude Code | [Connect it to Claude Code](../README.md#connect-it-to-claude-code) | ✔ 3/3 |
| 8. Call the API directly | [Check it works](../README.md#check-it-works) | ✔ 3/3 |
| 9. Check Claude Code can reach the server | [Check it works](../README.md#check-it-works) | ✔ 1/1 |
| 10. Ask for it in plain language, in a real Claude Code session | [Using it](../README.md#using-it) | ✔ 4/4 |
| 11. Usage, and who made each call | [Cost and usage](../README.md#cost-and-usage) | ✔ 2/2 |
| 12. Launch it from the JSON other clients use | [Other MCP clients](../README.md#other-mcp-clients) | ✔ 2/2 |
| 13. Update | [Install](../README.md#install) | ✔ 1/1 |

## 1. Quick start, in one go

README: [Quick start](../README.md#quick-start)

```console
$ git clone https://github.com/lhviet/jev-bridge.git && cd jev-bridge
$ mkdir -p ~/.jev-bridge && chmod 700 ~/.jev-bridge
$ printf 'TYPESAFE_API_KEY=%s\n' 'your-key-here' > ~/.jev-bridge/.env && chmod 600 ~/.jev-bridge/.env
$ claude mcp add --scope user jev -- node "$(pwd)/src/server.mjs"
$ node src/server.mjs --selftest   # one live call: proves the key works
```

Ran: the same, as a second new user, with the key in place of your-key-here; then claude mcp list

```text
Cloning into 'jev-bridge'...
Added stdio MCP server jev with command: node <clone>/src/server.mjs to user config
File modified: ~/.claude.json
key: loaded (108 chars, ••••••••…)
models: jev-latest, jev-preview
store: sqlite at ~/.jev-bridge/jev.db
{
  "model": "jev-1.13.0",
  "answers": {
    "is_urgent": {
      "type": "noul",
      "noul": 0.95
    },
    "department": {
      "type": "choice",
      "choice": "billing",
      "confidence": 0.8,
      "probabilities": {
        "sales": 0,
        "technical": 0.13,
        "billing": 0.87
      }
    },
    "frustration": {
      "type": "score",
      "score": 1.05,
      "confidence": 0.93,
      "legend": {
        "0": "Calm",
        "1": "Frustrated",
        "2": "Very angry"
      },
      "probabilities": {
        "0": 0,
        "1": 0.95,
        "2": 0.05
      }
    }
  },
  "usage": {
    "input_tokens": 399,
    "output_tokens": 73
  },
  "bridge": {
    "cached": false,
    "latency_ms": 178.14,
    "cost_usd": 0.000016758,
    "attempts": 1,
    "request_id": "req_01a0c429519e79fdae20fb7c1751e575",
    "call_id": "a2ad07ae-1"
  }
}
$ claude mcp list
Checking MCP server health…

jev: node <clone>/src/server.mjs - ✔ Connected
```

- ✔ the whole block exits 0
- ✔ the self-test loaded the key and got an answer from a jev model
- ✔ Claude Code lists jev as connected

## 2. Node.js 18 or newer

README: [Prerequisites](../README.md#prerequisites)

```console
$ node --version
```

```text
v24.21.0
```

- ✔ Node is 18 or newer

## 3. Clone

README: [Install](../README.md#install)

```console
$ git clone https://github.com/lhviet/jev-bridge.git
$ cd jev-bridge
```

Ran: git clone https://github.com/lhviet/jev-bridge.git jev-bridge

```text
Cloning into 'jev-bridge'...
$ git log -1 --oneline
2db4bbe [security|docs] Harden key handling and data files; Quick start; install guide and examples run as evidence (#2)
```

- ✔ the clone succeeded
- ✔ package.json declares no dependencies, so there is nothing to install

## 4. Run the tests

README: [Install](../README.md#install)

```console
$ npm test
```

```text
… (237 lines, summary below)
ℹ tests 171
ℹ suites 24
ℹ pass 171
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 6344.64375
```

- ✔ npm test exits 0
- ✔ no test failed

## 5. Note the server's absolute path

README: [Install](../README.md#install)

```console
$ echo "$(pwd)/src/server.mjs"
```

```text
<clone>/src/server.mjs
```

- ✔ the path exists

## 6. Write the key file

README: [Option A — a key file (recommended)](../README.md#option-a--a-key-file-recommended)

```console
$ mkdir -p ~/.jev-bridge && chmod 700 ~/.jev-bridge
$ printf 'TYPESAFE_API_KEY=%s\n' 'your-key-here' > ~/.jev-bridge/.env
$ chmod 600 ~/.jev-bridge/.env
```

Ran: the same, with the key in place of your-key-here

```text
$ ls -la ~/.jev-bridge
total 8
drwx------@ 3 <user>  staff   96 Sep 21 06:30 .
drwxr-xr-x@ 4 <user>  staff  128 Sep 21 06:30 ..
-rw-------@ 1 <user>  staff  126 Sep 21 06:30 .env
```

- ✔ ~/.jev-bridge is mode 700
- ✔ ~/.jev-bridge/.env is mode 600
- ✔ the bridge reads the key back from it

## 7. Register the server with Claude Code

README: [Connect it to Claude Code](../README.md#connect-it-to-claude-code)

```console
$ claude mcp add --scope user jev -- node /absolute/path/to/jev-bridge/src/server.mjs
```

```text
Added stdio MCP server jev with command: node <clone>/src/server.mjs to user config
File modified: ~/.claude.json
$ # what it wrote to ~/.claude.json
{
  "mcpServers": {
    "jev": {
      "type": "stdio",
      "command": "node",
      "args": [
        "<clone>/src/server.mjs"
      ],
      "env": {}
    }
  }
}
```

- ✔ claude mcp add exits 0
- ✔ ~/.claude.json has a user-scope "jev" server running node on the server's path
- ✔ the key is not in Claude Code's configuration

## 8. Call the API directly

README: [Check it works](../README.md#check-it-works)

```console
$ node src/server.mjs --selftest
```

```text
key: loaded (108 chars, ••••••••…)
models: jev-latest, jev-preview
store: sqlite at ~/.jev-bridge/jev.db
{
  "model": "jev-1.13.0",
  "answers": {
    "is_urgent": {
      "type": "noul",
      "noul": 0.95
    },
    "department": {
      "type": "choice",
      "choice": "billing",
      "confidence": 0.78,
      "probabilities": {
        "sales": 0,
        "billing": 0.85,
        "technical": 0.15
      }
    },
    "frustration": {
      "type": "score",
      "score": 1.05,
      "confidence": 0.92,
      "legend": {
        "0": "Calm",
        "1": "Frustrated",
        "2": "Very angry"
      },
      "probabilities": {
        "0": 0,
        "1": 0.95,
        "2": 0.05
      }
    }
  },
  "usage": {
    "input_tokens": 399,
    "output_tokens": 73
  },
  "bridge": {
    "cached": false,
    "latency_ms": 196.93,
    "cost_usd": 0.000016758,
    "attempts": 1,
    "request_id": "req_01a0c42974757a1c81abf74fb4bae767",
    "call_id": "cf5e121a-1"
  }
}
```

- ✔ the key was loaded from the key file
- ✔ a JSON answer came back from a jev model
- ✔ answers persist in ~/.jev-bridge/jev.db

## 9. Check Claude Code can reach the server

README: [Check it works](../README.md#check-it-works)

```console
$ claude mcp list
```

```text
Checking MCP server health…

jev: node <clone>/src/server.mjs - ✔ Connected
```

- ✔ jev is listed as connected

## 10. Ask for it in plain language, in a real Claude Code session

README: [Using it](../README.md#using-it)

```console
$ claude
```

Then, in the session:

```text
Use jev_ask to decide whether this support message is urgent and which team
should handle it: "Help! My payouts have been failing for 3 days."
```

Ran: claude -p "<that prompt>" --model sonnet --strict-mcp-config --mcp-config <the jev entry claude mcp add wrote> --allowedTools mcp__jev --settings '{"disableAllHooks":true}' --disable-slash-commands --output-format stream-json --verbose

```text
Claude Code 2.1.278 · claude-sonnet-5 · MCP server jev: connected
jev tools offered: mcp__jev__jev_ask, mcp__jev__jev_history, mcp__jev__jev_models, mcp__jev__jev_review, mcp__jev__jev_usage
tools Claude called: ToolSearch, ReadMcpResourceTool, mcp__jev__jev_ask

── the request Claude wrote ──
{
  "state": {
    "message": "Help! My payouts have been failing for 3 days."
  },
  "questions": {
    "urgent": {
      "type": "noul",
      "instructions": "Does the customer in `message` convey that the problem needs prompt attention (urgent)?",
      "criteria": {
        "true": "The customer signals urgency, distress, or ongoing harm that needs a fast response",
        "false": "The customer is asking a routine question or reporting something with no time pressure"
      }
    },
    "severity": {
      "type": "score",
      "instructions": "How severe is the problem described in `message` for the customer?",
      "criteria": [
        "Cosmetic or informational, no impact on the customer",
        "Degraded experience, the customer has a workaround",
        "Blocked, the customer cannot complete an important task and has no workaround",
        "Money is being lost, delayed, or withheld from the customer, or data is being lost"
      ]
    },
    "team": {
      "type": "choice",
      "instructions": "Which team should handle the issue in `message`?",
      "criteria": {
        "payments": "Payouts, transfers, bank accounts, transaction failures, settlement",
        "billing": "Invoices, subscription charges, refunds, plan pricing",
        "technical": "Bugs, outages, API or integration problems unrelated to money movement",
        "account": "Login, verification, identity checks, account settings",
        "sales": "Pricing questions, upgrades, new purchases",
        "none": "The message does not fit any of the above teams"
      }
    }
  }
}

── what Jev answered ──
urgent    noul    ███████████████████   0.95
severity  score   ███████████████████▉  2.99 of 3   confidence 0.99
team      choice  payments   confidence 1.00
            payments   ████████████████████  1.00
            account    ·                     0.00
            technical  ·                     0.00
            none       ·                     0.00
            sales      ·                     0.00
            billing    ·                     0.00
jev-1.13.0 · 211.73 ms · $0.0000253 · cached: false

── Claude's reply ──
**Verdict: urgent, route to the payments team.**

Jev's answers (call `219176e9-1`, model `jev-1.13.0`):

| Question | Answer | How sure |
|---|---|---|
| Urgent? | P(yes) = **0.95** | High |
| Severity (0–3 scale) | **2.99**, the top level: money delayed or withheld | Confidence 0.99 (99% on level 3) |
| Team | **payments** | Confidence 1.0 (the other options got about 0) |

Payouts that have failed for three days means the customer's money is stuck. Jev rated the urgency very high and had no doubt about the team.

You didn't give me your team list, so I made up the options: payments, billing, technical, account, sales and none. If your real queues differ, tell me the names and I'll re-run it. Also, 0.95 is well above any sensible cutoff (say 0.8), so I'd escalate without a human check.

Once you know whether the routing was right, I can record it with `jev_review` using the call ID above, so `jev_history` can track accuracy.

4 turns · 17.1 s · $0.0652 of Claude
```

- ✔ the session exits 0
- ✔ Claude Code connected to jev
- ✔ Claude called jev_ask
- ✔ jev_ask returned answers, not an error

## 11. Usage, and who made each call

README: [Cost and usage](../README.md#cost-and-usage)

```console
$ node src/server.mjs --stats 7
$ node src/server.mjs --history 7
```

```text
{
  "window_days": 7,
  "calls": 2,
  "live_calls": 2,
  "cache_hits": 0,
  "hit_rate": 0,
  "errors": 0,
  "input_tokens": 1001,
  "output_tokens": 161,
  "cost_usd": 0.000042042,
  "saved_input_tokens": 0,
  "saved_usd": 0,
  "avg_live_latency_ms": 204,
  "by_day": [
    {
      "day": "2026-09-21",
      "calls": 2,
      "cache_hits": 0,
      "input_tokens": 1001,
      "cost_usd": 0.000042042
    }
  ],
  "models": [
    {
      "resolved_model": "jev-1.13.0",
      "calls": 2
    }
  ],
  "cache": {
    "entries": 2,
    "bytes": 45056,
    "ttl_days": 7,
    "max_entries": 20000
  },
  "store": "sqlite"
}
$ node src/server.mjs --history 7   # the calls, one per line
219176e9-1  claude-code        live    status 200
cf5e121a-1  —                  live    status 200
```

- ✔ both live calls were recorded: --selftest and the session
- ✔ the history files the session's call under claude-code

## 12. Launch it from the JSON other clients use

README: [Other MCP clients](../README.md#other-mcp-clients)

```json
{
  "mcpServers": {
    "jev": {
      "command": "node",
      "args": ["/absolute/path/to/jev-bridge/src/server.mjs"]
    }
  }
}
```

Ran: the command and args from that JSON, launched over stdio: initialize, then tools/list

```text
serverInfo: {"name":"jev-bridge","title":"Jev (TypeSafe System One)","version":"0.1.0","description":"Calibrated, typed judgments from TypeSafe's Jev, with a local answer cache, cost accounting and a reviewable call history.","websiteUrl":"https://github.com/lhviet/jev-bridge"}
protocolVersion: 2025-11-25
tools: jev_ask, jev_usage, jev_history, jev_review, jev_models
```

- ✔ the server answers initialize
- ✔ all five tools are listed

## 13. Update

README: [Install](../README.md#install)

```console
$ git -C /path/to/jev-bridge pull
```

```text
Already up to date.
```

- ✔ git pull succeeds

## Reproduce

```bash
node evidence/install.mjs                 # from GitHub: needs git, a logged-in claude and a TypeSafe key
node evidence/install.mjs --source .      # from this checkout's committed HEAD instead
```
